#include "stdafx.h"
#include "DXContext.h"

bool DXContext::Init()
{
    /*
    if (FAILED(CreateDXGIFactory2(0, IID_PPV_ARGS(m_dxgiFactory)))) {
        return false;

    }
    */
    return false;
}
